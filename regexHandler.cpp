#include <string>
#include <regex>
#include "regexHandler.h"

using namespace std;

bool isMatch(string match, string str) {

    regex = match;
    reg1(regex);

    return regex_search(str, reg1);

}
